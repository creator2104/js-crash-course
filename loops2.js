teacollection = ['green tea','black tea','chai','oolong tea']
selectedTeas = []
for (let i = 0; i < teacollection.length ; i++) {
    const element = array[i];
    while (i != "chai") {
        selectedTeas.push
    }
}